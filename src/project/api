// Массив элементов, описывающих API (квадратные скобки опускаются).
// Обязательным является лишь свойство "start".
{
    //path: 'play.sh',
    start: 'startPlayer', // это же имя для скрипта(программы), если целевая платформа - не веб; это же функция для запуска процесса (если веб, то ничего не делает, но будет ругаться при вызовах input-функции)
    input: 'setToPlayer',
    output: 'playerChanged',
    finished: 'playerFinished'
}
